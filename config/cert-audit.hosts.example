# Certificate audit host list
# format: host:port:label
# port defaults to 443 if omitted
# label is used for display only

home.example.tld:1688:Freebox OS
mail.example.tld:443:Mail / HTTPS
vpn.example.tld:443:VPN gateway
nas.example.tld:5001:NAS admin
